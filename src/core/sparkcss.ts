// import { lexer } from "../lexer/lexer";
// import { parse } from "../parser/parser";
// import fs from "fs";


// TODO: Write The code generator to convert Spark code into valid CSS

// export function processSparkFile(inputPath: string, outputPath: string) {
//   const input = fs.readFileSync(inputPath, "utf-8");
//   const tokens = lexer(input);
//   const cssOutput = parse(tokens);
  
//   fs.writeFileSync(outputPath, cssOutput);
//   console.log(`Processed ${inputPath} and generated CSS in ${outputPath}`);
// }

// // Example usage
// processSparkFile("input.spark", "output.css");
